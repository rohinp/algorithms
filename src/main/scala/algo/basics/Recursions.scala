package algo.basics

object Recursions:
  /* 

    The assense of recursion is prety simple, 
      1. finding the base case(r) or termination condition(s)
      2. finding the repetative task(s) in the algorithm

    Though for some problem statements it is simple to find the repetative tasks.
    But in some cases it might be difficult to get it, but with some practice it becomes 
    quite obvious. Even we can say that at some point 
  */
  println("")